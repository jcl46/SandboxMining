using UnityEngine;
using System.Collections;

public class DragAndDropItemScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    public void InventorySlotClicked(int InventorySlotIndex)
    { // An inventory slot was clicked

        // TODO: If an item is being dragged, put it into the inventory (or swap it with the inventory slot item)
        // TODO: If an item is not being dragged, start dragging the inventory item
    }
}
